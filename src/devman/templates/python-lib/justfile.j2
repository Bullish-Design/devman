{% if use_just %}
default: dev

dev:
	uv run pytest -q

test:
	uv run pytest -q

fmt:
	ruff format .

lint:
	ruff check .

docker-build:
	docker build -t { '{ project_slug }' } .

docker-up:
	docker compose up -d

docker-down:
	docker compose down
{% endif %}
