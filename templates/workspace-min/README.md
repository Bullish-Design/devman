# workspace-min

This template provides a minimal `.devman/` workspace for devman.

## Usage

1. Copy this template into a new project.
2. Run `devenv up` or `direnv allow` to enter the dev environment.
3. Launch `devman` from the project root.
