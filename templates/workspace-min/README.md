# workspace-min

This template provides a minimal `.devman/` workspace for llm-core.

## Usage

1. Copy this template into a new project.
2. Run `devenv up` or `direnv allow` to enter the dev environment.
3. Launch `llm-core` from the project root.
