[project]
name = "{{ project_name }}"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">= {{ python_version }}"
dependencies = []

[tool.uv]
dev-dependencies = [
  "pytest>=8.0.0",
  "ruff>=0.5.0",
]

[tool.ruff]
target-version = "py{{ python_version | replace('.', '') }}"
