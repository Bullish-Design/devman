# Agent factory pre-implementation issue register

Date: 2026-08-20

Likelihood, impact, and detectability use `low`, `medium`, or `high`.
Detectability means how likely current checks are to detect the failure.

| ID | Subsystem and invariant | Failure mode and trigger | L / I / D | Current evidence and reproducer | Status | Mitigation and owner | Residual risk, confidence, evidence to change |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AF-001 | Truth model: one accepted owner per field | Exact source stored as `spec` makes source, not semantic intent, the durable truth | high / high / high | Spike F `source_spec()` stores `source_contract`; `agent-factory-round-trip/prototype.py` | blocker | Define bootstrap-only records and an exit gate; agent-factory design | High residual risk; high confidence; needs meaningful semantic records that re-derive a blinded corpus |
| AF-002 | Examples: authored examples must gate intended behavior | Missing, stale, weak, or correlated examples accept wrong behavior | high / high / medium | `CONCEPT.md` Section 10.3 states the bound; no quality corpus exists | unknown | Define executable example schema, mutation checks, and staleness rules; agent-factory design | High residual risk; medium confidence; needs false-accept and false-reject measurements |
| AF-003 | Templateer: raw Python fragments preserve syntax and bytes | `language: python` escapes source strings as quoted-string content | high / high / high | `20260820T191030Z-templateer-probe/results.json`; current render fails; mixed text template injects structure | bounded-risk | Remove Templateer from final Python assembly; keep the executable first-class fragment contract for Templateer; agent-factory and Templateer | Low residual risk at this boundary; high confidence; support corpus must prove the external renderer |
| AF-004 | Promotion: accepted spec captures the source edit's meaning | Model output is malformed, partial, hallucinated, stale, or repeatedly divergent | high / high / medium | `20260820T192215Z-promotion-probe-retry/results.json`: 10 accepts, 9 rejects, zero orchestration false decisions; model quality untested | blocker | Run `MODEL_QUALITY_PROTOCOL.md` with an authorized local provider; agent-factory | High residual risk; high confidence; deterministic routing is bounded, but semantic truth needs blinded measurement |
| AF-005 | Python preservation: supported constructs remain byte-identical | Abstract syntax tree spans omit trivia or ownership for comments and complex forms | high / high / medium | Small fixture passed; decorators required a one-byte span repair | unknown | Categorized corpus; add token or concrete syntax tree boundary where required; agent-factory/pydantree contract | High residual risk; high confidence; needs full support-matrix evidence |
| AF-006 | Syntax safety: invalid buffers never mutate truth | Tree-sitter can extract around `ERROR` or missing nodes unless the caller rejects the tree | medium / high / medium | pydantree exposes raw parse errors and leaves extraction choice to caller | bounded-risk | Require a pre-extraction tree error walk; agent-factory | Low residual risk after test; high confidence; needs invalid-buffer corpus |
| AF-007 | Identity: durable identity is independent of path and name | Duplicate bodies or competing targets cause false rename/move continuity | high / high / medium | Spike F fingerprint bridge accepts only one-to-one matches; Spike E name bridge was scoped to public source | blocker | Evidence-ranked identity policy with proposal/human boundary; agent-factory | High residual risk; high confidence; needs adversarial metrics |
| AF-008 | Imports and ordering preserve Python semantics | Sorting or deduplicating side-effect or conditional imports changes behavior | high / high / low | Spike F sorts a narrow absolute-import subset and rejects one collated edit | unknown | Explicit preamble ownership and unsupported import matrix; agent-factory | High residual risk; high confidence; needs adversarial import corpus |
| AF-009 | Persistence: accepted store and source change as one logical transaction | Partial store or multi-file source write creates split-brain state | medium / critical / medium | fsdantic CAS is per key; `save_many()` is per-item and non-atomic | blocker | Journaled state machine with recovery; agent-factory, with possible fsdantic contract | High residual risk; high confidence; needs interruption tests |
| AF-010 | Crash safety: materialization preserves last accepted tree | Process stops between target-to-backup and staging-to-target renames | medium / high / medium | fsdantic restores one orphaned backup; docs call recovery best-effort and non-concurrent | bounded-risk | Test exact interruption points and restrict supported filesystem boundary; agent-factory/fsdantic contract | Medium residual risk; high confidence; needs fresh crash probes |
| AF-011 | Determinism: semantic inputs produce identical bytes | Renderer, formatter, version, traversal order, locale, or newline changes bytes | medium / high / high | Spike F repeated one process; Spike A proved byte-sensitive rebuild costs | unknown | Complete input manifest, cache key, cross-process hash matrix; agent-factory | Medium residual risk; high confidence; needs cold/warm varied-order evidence |
| AF-012 | Migration: existing repositories adopt without unbounded review | Unsupported syntax, generated files, monorepos, or history churn creates excessive manual work | high / high / medium | No real agent-factory history replay exists | unknown | Disposable multi-repo replay and preview/rollback design; agent-factory | High residual risk; high confidence; needs sampled history metrics |
| AF-013 | Performance: ordinary saves meet a derived latency budget | Full-tree parse, render, Ruff, or model context hides behind unit-level language | high / high / high | No pre-implementation performance baseline exists | unknown | Component timings and size curves; agent-factory | High residual risk; high confidence; needs small/medium/large measurements |
| AF-014 | Security: generation does not imply execution | Hostile source, template, model output, paths, symlinks, commands, or logs cross trust boundaries | medium / critical / low | Templateer validates paths and syntax; schema modules and command validators execute code | unknown | Threat model, negative probes, explicit execution boundary, redaction rules; multiple owners | High residual risk; high confidence; needs negative test matrix |
| AF-015 | Developer experience: the tool does not fight common repository operations | Save hooks, formatters, conflicts, branch changes, or removal cause loops or loss | high / high / medium | Only the generation-token idea exists; no workflow walkthrough is measured | unknown | State diagrams, status/explain/repair/export/remove contracts; agent-factory | High residual risk; medium confidence; needs disposable workflow rehearsals |
| AF-016 | Compatibility: version and platform boundaries are explicit | Python syntax, parser, Ruff, filesystem, case, or symlink behavior changes outcomes | medium / high / medium | Current evidence covers Linux, Python 3.13.14, Ruff 0.15.20, pydantree 0.2.0, Templateer 0.2.0 | unknown | Compatibility matrix and bounded platform claims; agent-factory and sibling owners | Medium residual risk; high confidence; needs version/platform probes |
| AF-017 | Concurrency prerequisites: generation token and compare-and-set semantics are complete | A later stress test measures undefined single-process behavior | high / high / high | Prompt forbids stress until ownership, transactions, persistence, and token rules close | deferred-with-owner | Define races, locks, ordering, cancellation, recovery, and entry gates; later concurrency spike | High residual risk; high confidence; status changes after AF-003 through AF-011 close |
