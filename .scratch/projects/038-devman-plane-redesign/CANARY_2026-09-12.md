# Project 038 wider canary — 2026-09-12

The canary used the 46 real manifest roots and the live state directory
`$HOME/.local/state/vendomat/devman`. Each category ran `vendomat plane plan`
followed by `vendomat plane update` with the complete real project set. Each
update was a no-op and kept generation 2 active.

| Category | Repository and workflow | Plan | Update | Generation assertion |
|---|---|---:|---:|---|
| Local overlay | `devman.agent-review` | 0 | 0 | Passed. Metadata records group `overlay`; the overlay DAG exists. |
| Multiple groups | `observantic.release` | 0 | 0 | Passed. Metadata records `base`, `release`; the release DAG exists. |
| Cross repository | `devman.stack-validate` | 0 | 0 | Passed. The DAG contains `dag.run` and `OBSERVANTIC_DIR`. |
| Scheduled workflow | `observantic.maintain` | 0 | 0 | Passed. The DAG contains `schedule: "5 0 * * *"`. |
| Declared writes | `devman.format` | 0 | 0 | Passed. Metadata records `insitu`; the DAG contains the format receipt path. |
| Unusual path | `loci.nvim.check` | 0 | 0 | Passed. Metadata records `/home/andrew/Documents/Projects/loci.nvim`; the DAG exists. |

The exact loop returned `canary_overall=passed`. The full generation contained
46 projects and 146 DAGs. One validation loop returned
`validated_dags=146`.

Live checks used the environment-cleared commands required by the machine
cutover:

```sh
env -u PYTHONPATH -u NIX_PYTHONPATH /run/current-system/sw/bin/devman doctor
env -u PYTHONPATH -u NIX_PYTHONPATH dagu --dagu-home "$HOME/.local/share/dagu" ls
```

`dagu ls` returned 146 entries and exit 0. Doctor reported `mode plane` and
exit 1 with five findings:

1. `flora-037-part-e` link drift: `devenv.local.nix` needs `create`.
2. `shellij` has uncommitted changes consumed by four projects through an
   unpinned local input.
3. `vendomat` has uncommitted changes consumed by two projects through an
   unpinned local input.
4. `repoman` has uncommitted changes consumed by one project through an
   unpinned local input.
5. One Dagu process has `SHELL` set; `default_shell` governs it.

These are findings, not canary failures. No new task was launched because the
declared-write and agent categories would alter real repository state, and the
cross-repository category would enqueue child workflows. Run and metadata
behavior remains covered by the existing Dagu VM proof and the exact generated
DAG comparison.

After the migration commits, the complete plan/update was rerun with all 46
manifest roots. The plan proposed generation 3, and the unchanged update
correctly retained active generation 2 as a no-op. A single shell loop
validated all 146 active DAGs. Vendomat's required
`devenv shell -- testee verify --mode quick` then passed: ruff, format, ty,
and pytest all passed in 34.2 seconds.

The final environment-cleared doctor still reported `mode: plane` and exit 1.
It reported four findings: `flora-037-part-e` link drift, uncommitted local
Vendomat and RepoMan sources consumed by unpinned inputs, and one Dagu process
with `SHELL` set. The earlier `shellij` dirty-source finding disappeared after
its migration commit. The doctor top line remains `4 projects, 16 workflows`
because it reads the independent old state-registration directory; Dagu still
lists all 146 plane workflows.
